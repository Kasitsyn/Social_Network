let initialSatate = {}

type InitialSatateType = typeof initialSatate

const sidebarReducer = (state = initialSatate, action: any): InitialSatateType => {

    return state
}

export default sidebarReducer